const CustomLink = ({ url, title })=> {
    return (
        <> 
            <span className="hover:underline text-[17px] hover:scale-110 cursor-pointer">{title}</span>
        </>
    )
}

export default CustomLink; 